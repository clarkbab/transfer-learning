import fire
import os
import sys

root_dir = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..'))
sys.path.append(root_dir)

from mymi.prediction.dataset.dicom import create_two_stage_predictions

fire.Fire(create_two_stage_predictions)

# Sample args:
# --dataset PMCC-HN --localiser "(...)" --segmenter "(...)" --clear_cache False --region Parotid_L
